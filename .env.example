# Copy to .env and fill in. Never commit .env.
# World Labs Marble API key — used by scripts/marble.ts to generate/download worlds.
# Generating worlds spends shared credits; ask in the team channel before running generate.
MARBLE_API_KEY=
