# Organizer rules question — send by Jul 8 (Gate B)

**To:** organizers (SensAI Hackademy / Poeia XR / Machine Cinema / FBRC.AI / AI LA)
**Subject:** Pre-event open-source library — permitted under the rules?

---

Hi [organizer name],

We're registered for the Worlds in Action hackathon and want to make sure our
preparation stays inside the rules before we start.

Our plan:

1. **Before the event**, publish a generic open-source library (MIT/Apache,
   public repo with dated commits) for surveying 3D world files — loading
   splat + collider pairs, running physics probes, taking measurements. It is
   general-purpose infrastructure, not the event app.
2. **At the event**, build the actual application (UI, demo flow, agent
   choreography) from scratch on-site, consuming that public library the same
   way we'd consume any open-source dependency, with public commits from hour
   zero of the build window.

Two questions:

1. Is pre-event work structured this way — a disclosed, public, open-source
   library plus on-site application development — permitted?
2. Is there anything in the judging criteria that treats pre-event
   open-source dependencies authored by the team differently from third-party
   dependencies?

We'll disclose this structure again in one sentence during our pitch. If any
part of it isn't allowed, we'd rather know now and adjust scope than have a
problem at judging.

Thanks — looking forward to the event.

[name / team]

---

**Tracking:** sent ____ · answer due **Jul 8 EOD** · on "no"/silence →
execute `docs/contingency-28h.md` the same day (kill criterion, pre-committed).
